package quizora.DAO;

/** Admin-only management of teacher accounts. */
public final class TeacherManagementDAO extends AccountManagementDAO {
    public TeacherManagementDAO() { super("teacher"); }
}
